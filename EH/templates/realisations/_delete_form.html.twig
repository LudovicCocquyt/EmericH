<form method="post" class="text-right mr-5" action="{{ path('realisations_delete_image', {'id': realisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realisation.id) }}">
    <button class="btn"><i class="fas fa-trash-alt red-text"></i></button>
</form>
